require "spec_helper"

RSpec.describe Logga do
  it "has a version number" do
    expect(Logga::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).not_to eq(true)
  end
end
